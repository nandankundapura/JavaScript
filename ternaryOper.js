let age=18;
let message;
if(age>=18)
{
    message="can vote";
}
else
{
    message="cannot vote";
}
console.log(message);

//using ternary operator
//syntax :  condition ? expression1 : expression2

let Age=16;
let Message;

Message=Age>=18 ? "can vote" : "cannot vote";
console.log(Message);

